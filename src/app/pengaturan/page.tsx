"use client";

// This page is now a directory. The content has been moved to sub-pages.
// A redirect is in place in next.config.js to handle navigation.
export default function PengaturanRedirectPage() {
    return null;
}
